// Standalone image generator utility — only works server-side with a GEMINI_API_KEY set.
async function generateHeroImage(): Promise<string | null> {
  return null;
}

export default generateHeroImage;
